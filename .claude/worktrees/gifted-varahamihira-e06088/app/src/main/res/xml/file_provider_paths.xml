<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- For files in getExternalFilesDir() -->
    <external-files-path
        name="external_files"
        path="." />

    <!-- Optional: support cache directory if needed -->
    <external-cache-path
        name="external_cache"
        path="." />

    <!-- Optional: public Pictures directory -->
    <external-path
        name="external_public"
        path="." />

</paths>